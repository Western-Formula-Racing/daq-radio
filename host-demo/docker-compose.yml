version: '3.8'

services:
  pecan-dashboard:
    build:
      context: ..
      dockerfile: host-demo/Dockerfile
    container_name: pecan-demo
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    environment:
      - NODE_ENV=production
