# Startup data loader

> **Removed.** The `startup-data-loader` service no longer exists in `installer/docker-compose.yml`. The referenced directory (`installer/startup-data-loader/`) and its sample data have been removed from the repository.
>
> To seed TimescaleDB with sample data, upload a CSV manually via the **file-uploader** service at `http://localhost:8084`.
